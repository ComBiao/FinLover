# npm's Arborist peer-dependency resolver currently crashes on this project's
# dependency graph with "Cannot read properties of null (reading 'edgesOut')"
# during `npm install` (reproduced on npm 10.2.3 and 10.9.3). Forcing legacy
# (npm v6-style) peer dependency resolution avoids the crash.
legacy-peer-deps=true
